[project]
name = "tensorlake-agent"
version = "0.1.0"
description = "TensorLake Document AI agent for parsing documents and extracting structured data"
requires-python = ">=3.10, <3.13"
dependencies = [
    "agentuity>=0.0.106",
    "tensorlake>=0.1.0",
    "pydantic>=2.0.0",
]
