import { ReadableStream } from 'node:stream/web';
import type { Data, ReadableDataType, Json } from '../types';
import { type Email } from '../io/email';
/**
 * The implementation of the Data interface
 */
export declare class DataHandler implements Data {
    private readonly _type;
    private _readstream?;
    private _buffer;
    private _email?;
    constructor(stream: string | Buffer | ReadableStream<ReadableDataType> | AsyncIterable<ReadableDataType>, contentType: string);
    private ensureStreamLoaded;
    toString(): string;
    private data;
    get contentType(): string;
    base64(): Promise<string>;
    text(): Promise<string>;
    json(): Promise<Json>;
    object<T>(): Promise<T>;
    binary(): Promise<Uint8Array>;
    blob(): Promise<Blob>;
    arrayBuffer(): Promise<ArrayBuffer>;
    stream(): Promise<ReadableStream<ReadableDataType>>;
    email(): Promise<Email>;
    private isTextChunkable;
    buffer(): Promise<Buffer>;
}
//# sourceMappingURL=data.d.ts.map