// @generated
// Auto-generated by Agentuity - DO NOT EDIT
import dynamicTools from '../agent/dynamic-tools/index.js';
import type { AgentRunner } from '@agentuity/runtime';
import type { InferInput, InferOutput } from '@agentuity/core';

// ============================================================================
// Schema Type Exports
// ============================================================================

/**
 * Input type for dynamic-tools agent
 * LangChain agent with dynamic tool filtering by state, context, and runtime registration
 */
export type DynamicToolsInput = InferInput<typeof dynamicTools['inputSchema']>;

/**
 * Output type for dynamic-tools agent
 * LangChain agent with dynamic tool filtering by state, context, and runtime registration
 */
export type DynamicToolsOutput = InferOutput<typeof dynamicTools['outputSchema']>;

/**
 * Input schema type for dynamic-tools agent
 * LangChain agent with dynamic tool filtering by state, context, and runtime registration
 */
export type DynamicToolsInputSchema = typeof dynamicTools['inputSchema'];

/**
 * Output schema type for dynamic-tools agent
 * LangChain agent with dynamic tool filtering by state, context, and runtime registration
 */
export type DynamicToolsOutputSchema = typeof dynamicTools['outputSchema'];

/**
 * Agent type for dynamic-tools
 * LangChain agent with dynamic tool filtering by state, context, and runtime registration
 */
export type DynamicToolsAgent = AgentRunner<
	DynamicToolsInputSchema,
	DynamicToolsOutputSchema,
	typeof dynamicTools['stream'] extends true ? true : false
>;

// ============================================================================
// Agent Definitions
// ============================================================================

/**
 * Agent Definitions
 * 
 * Registry of all agents in this application.
 * Provides strongly-typed access to agent metadata and runner functions.
 * 
 * @remarks
 * This object is auto-generated from your agent files during build.
 * Each agent has corresponding Input, Output, and Runner types exported above.
 * 
 * @example
 * ```typescript
 * import { AgentDefinitions, SessionBasicInput } from './generated/registry';
 * 
 * // Access agent definition
 * const agent = AgentDefinitions.sessionBasic;
 * 
 * // Use typed schema types
 * const input: SessionBasicInput = { ... };
 * const result = await agent.run(input);
 * ```
 */
export const AgentDefinitions = {
	/**
	 * dynamic-tools
	 * LangChain agent with dynamic tool filtering by state, context, and runtime registration
	 * @type {DynamicToolsAgent}
	 */
	dynamicTools,
} as const;

// ============================================================================
// Module Augmentation
// ============================================================================

// Augment @agentuity/runtime types with strongly-typed agents from this project
declare module "@agentuity/runtime" {
	// Augment the AgentRegistry interface with project-specific strongly-typed agents
	export interface AgentRegistry {
		dynamicTools: DynamicToolsAgent;
	}
}

// FOUND AN ERROR IN THIS FILE?
// Please file an issue at https://github.com/agentuity/sdk/issues
// or if you know the fix please submit a PR!
