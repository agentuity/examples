// @generated
// Auto-generated by Agentuity - DO NOT EDIT
import structuredContext from '../agent/structured-context/index.js';
import type { AgentRunner } from '@agentuity/runtime';
import type { InferInput, InferOutput } from '@agentuity/core';

// ============================================================================
// Schema Type Exports
// ============================================================================

/**
 * Input type for structured-context agent
 * OpenAI Agents SDK agent with RunContext<T> for typed context and outputType for structured JSON output
 */
export type StructuredContextInput = InferInput<typeof structuredContext['inputSchema']>;

/**
 * Output type for structured-context agent
 * OpenAI Agents SDK agent with RunContext<T> for typed context and outputType for structured JSON output
 */
export type StructuredContextOutput = InferOutput<typeof structuredContext['outputSchema']>;

/**
 * Input schema type for structured-context agent
 * OpenAI Agents SDK agent with RunContext<T> for typed context and outputType for structured JSON output
 */
export type StructuredContextInputSchema = typeof structuredContext['inputSchema'];

/**
 * Output schema type for structured-context agent
 * OpenAI Agents SDK agent with RunContext<T> for typed context and outputType for structured JSON output
 */
export type StructuredContextOutputSchema = typeof structuredContext['outputSchema'];

/**
 * Agent type for structured-context
 * OpenAI Agents SDK agent with RunContext<T> for typed context and outputType for structured JSON output
 */
export type StructuredContextAgent = AgentRunner<
	StructuredContextInputSchema,
	StructuredContextOutputSchema,
	typeof structuredContext['stream'] extends true ? true : false
>;

// ============================================================================
// Agent Definitions
// ============================================================================

/**
 * Agent Definitions
 * 
 * Registry of all agents in this application.
 * Provides strongly-typed access to agent metadata and runner functions.
 * 
 * @remarks
 * This object is auto-generated from your agent files during build.
 * Each agent has corresponding Input, Output, and Runner types exported above.
 * 
 * @example
 * ```typescript
 * import { AgentDefinitions, SessionBasicInput } from './generated/registry';
 * 
 * // Access agent definition
 * const agent = AgentDefinitions.sessionBasic;
 * 
 * // Use typed schema types
 * const input: SessionBasicInput = { ... };
 * const result = await agent.run(input);
 * ```
 */
export const AgentDefinitions = {
	/**
	 * structured-context
	 * OpenAI Agents SDK agent with RunContext<T> for typed context and outputType for structured JSON output
	 * @type {StructuredContextAgent}
	 */
	structuredContext,
} as const;

// ============================================================================
// Module Augmentation
// ============================================================================

// Augment @agentuity/runtime types with strongly-typed agents from this project
declare module "@agentuity/runtime" {
	// Augment the AgentRegistry interface with project-specific strongly-typed agents
	export interface AgentRegistry {
		structuredContext: StructuredContextAgent;
	}
}

// FOUND AN ERROR IN THIS FILE?
// Please file an issue at https://github.com/agentuity/sdk/issues
// or if you know the fix please submit a PR!
