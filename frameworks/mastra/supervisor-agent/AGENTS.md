# Agents

This project contains the following agents:

## supervisor-agent

A multi-agent supervisor system that coordinates a copywriter agent and an editor agent to create high-quality blog posts. The publisher agent manages the workflow, calling the copywriter to generate initial content and the editor to refine it.

### Features

- Multi-agent coordination pattern
- Specialized agents for copywriting and editing
- Tool-based agent communication
- Supervised workflow execution
