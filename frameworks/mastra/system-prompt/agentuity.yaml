# yaml-language-server: $schema=https://raw.githubusercontent.com/agentuity/cli/refs/heads/main/agentuity.schema.json

# ------------------------------------------------
# This file is generated by Agentuity
# You should check this file into version control
# ------------------------------------------------

# The version semver range required to run this project
version: '>=0.0.130'
# The name of the project which is editable
name: mastra-system-prompt
# The description of the project which is editable
description: "Mastra example demonstrating changing the system prompt, wrapped with Agentuity."
# The development configuration for the project
development:
  port: 3500
  watch:
    enabled: true
    files:
      - src/**
  command: bun
  args:
    - run
    - --silent
    - .agentuity/index.js
deployment:
  command: bun
  args:
    - run
    - --no-install
    - --prefer-offline
    - --silent
    - --no-macros
    - --no-global-search-paths
    - --report-uncaught-exception
    - --disable-sigusr1
    - --disallow-code-generation-from-strings
    - --no-addons
    - --no-deprecation
    - .agentuity/index.js
  resources:
    memory: 250Mi
    cpu: 500M
    disk: 100Mi
bundler:
  enabled: true
  identifier: bunjs
  language: javascript
  runtime: bunjs
  agents:
    dir: src/agents
  ignore:
    - node_modules/**
    - dist/**
    - src/**
agents:
  - name: mastra-system-prompt
