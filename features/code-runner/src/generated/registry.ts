// @generated
// Auto-generated by Agentuity - DO NOT EDIT
import codeRunner from '../agent/code-runner/agent.js';
import type { AgentRunner } from '@agentuity/runtime';
import type { InferInput, InferOutput } from '@agentuity/core';

// Eval file imports (side-effect imports to register evals via createEval)
import '../agent/code-runner/eval.js';

// ============================================================================
// Schema Type Exports
// ============================================================================

/**
 * Input type for code-runner agent
 * Takes a coding prompt, generates TypeScript and Python implementations, and executes both in parallel sandboxes
 */
export type CodeRunnerInput = InferInput<typeof codeRunner['inputSchema']>;

/**
 * Output type for code-runner agent
 * Takes a coding prompt, generates TypeScript and Python implementations, and executes both in parallel sandboxes
 */
export type CodeRunnerOutput = InferOutput<typeof codeRunner['outputSchema']>;

/**
 * Input schema type for code-runner agent
 * Takes a coding prompt, generates TypeScript and Python implementations, and executes both in parallel sandboxes
 */
export type CodeRunnerInputSchema = typeof codeRunner['inputSchema'];

/**
 * Output schema type for code-runner agent
 * Takes a coding prompt, generates TypeScript and Python implementations, and executes both in parallel sandboxes
 */
export type CodeRunnerOutputSchema = typeof codeRunner['outputSchema'];

/**
 * Agent type for code-runner
 * Takes a coding prompt, generates TypeScript and Python implementations, and executes both in parallel sandboxes
 */
export type CodeRunnerAgent = AgentRunner<
	CodeRunnerInputSchema,
	CodeRunnerOutputSchema,
	typeof codeRunner['stream'] extends true ? true : false
>;

// ============================================================================
// Agent Definitions
// ============================================================================

/**
 * Agent Definitions
 * 
 * Registry of all agents in this application.
 * Provides strongly-typed access to agent metadata and runner functions.
 * 
 * @remarks
 * This object is auto-generated from your agent files during build.
 * Each agent has corresponding Input, Output, and Runner types exported above.
 * 
 * @example
 * ```typescript
 * import { AgentDefinitions, SessionBasicInput } from './generated/registry';
 * 
 * // Access agent definition
 * const agent = AgentDefinitions.sessionBasic;
 * 
 * // Use typed schema types
 * const input: SessionBasicInput = { ... };
 * const result = await agent.run(input);
 * ```
 */
export const AgentDefinitions = {
	/**
	 * code-runner
	 * Takes a coding prompt, generates TypeScript and Python implementations, and executes both in parallel sandboxes
	 * @type {CodeRunnerAgent}
	 */
	codeRunner,
} as const;

// ============================================================================
// Module Augmentation
// ============================================================================

// Augment @agentuity/runtime types with strongly-typed agents from this project
declare module "@agentuity/runtime" {
	// Augment the AgentRegistry interface with project-specific strongly-typed agents
	export interface AgentRegistry {
		codeRunner: CodeRunnerAgent;
	}
}

// FOUND AN ERROR IN THIS FILE?
// Please file an issue at https://github.com/agentuity/sdk/issues
// or if you know the fix please submit a PR!
